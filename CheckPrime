import math
number = int(input())
prime = False
for i in range(2,int(math.ceil(math.sqrt(number)))):
    if number%i ==0:
        prime = True

if prime:
    print(number, "is not a prime numer")
else:
    print(number, "is a prime numer")
    

